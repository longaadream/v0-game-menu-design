{
  "id": "rule-reap",
  "name": "收割",
  "description": "（被动）造成伤害时，对自己造成伤害量50%的治疗",
  "trigger": {
    "type": "afterDamageDealt"
  },
  "effect": {
    "type": "triggerSkill",
    "skillId": "reap-heal",
    "message": "${source.name}触发收割，恢复${Math.floor(damage * 0.5)}点生命值"
  }
}
